{% extends "layout.njk" %}

{% block content %}

<section class="shop container">
  <h2>Últimos Produtos</h2>

  <div class="items">
    {% for product in products %}
      <div class="product">
        <div class="image"
          style:"background: url({{"
        >

        </div>
      </div>
    {% endfor %}
  </div>
</section>

{% endblock %}}